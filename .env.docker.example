# Optional non-secret host-port overrides for compose.yml. Use .env.example as
# the complete variable inventory; keep working credentials only in ignored .env.
VYMPEL_POSTGRES_HOST_PORT=5433
VYMPEL_REDIS_HOST_PORT=6379
VYMPEL_MINIO_API_HOST_PORT=9100
VYMPEL_MINIO_CONSOLE_HOST_PORT=9101
VYMPEL_BACKEND_HOST_PORT=8080
VYMPEL_PUBLIC_HOST_PORT=3200
VYMPEL_CRM_HOST_PORT=3201
